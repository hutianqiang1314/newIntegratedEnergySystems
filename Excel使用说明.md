# Excel数据收集模板使用说明

## 导入CSV文件到Excel

1. **打开Excel**
2. **导入CSV文件**：
   - 点击"数据" > "获取数据" > "来自文本/CSV"
   - 选择 `data_collection_template.csv` 文件
   - 选择分隔符为"逗号"
   - 点击"加载"

## 格式设置建议

### 1. 列宽调整
- 分类列：宽度 15
- 子分类列：宽度 12
- 参数名列：宽度 20
- 参数描述列：宽度 25
- 其他列：宽度 12

### 2. 表头格式
```
字体：微软雅黑，加粗
背景色：浅蓝色 (RGB: 217, 225, 242)
对齐：居中
```

### 3. 数据验证设置

#### 必需字段验证
在"必需"列设置数据验证：
- 来源：是,可选
- 输入信息："请选择是否为必需参数"

#### 布尔值字段验证
在相关参数的"实际值"列设置：
- 来源：true,false
- 输入信息："请选择true或false"

#### 数值范围验证
为数值参数设置范围验证，例如：
- 效率值：0到1之间的小数
- 比例值：0到1之间的小数，且相关比例总和=1

### 4. 条件格式设置

#### 必需参数高亮
```
条件：$H$2:$H$100 = "是"
格式：浅红色背景 (RGB: 255, 199, 206)
```

#### 已填写参数标记
```
条件：$I$2:$I$100 <> ""
格式：浅绿色背景 (RGB: 198, 239, 206)
```

### 5. 冻结窗格
冻结前4列（分类到示例值），方便查看参数信息

## 数据填写工作表

### 创建分离的数据填写表
建议创建第二个工作表"数据填写"，包含以下列：
1. 参数名
2. 参数描述
3. 单位
4. 实际值
5. 数据来源
6. 填写日期
7. 负责人
8. 备注

### 时序数据处理
对于需要24个小时数据的参数，建议：
1. 创建单独的工作表"时序数据"
2. 参数名作为列标题
3. 小时0-23作为行标题
4. 在主表中引用对应数据

## 数据验证功能

### 1. 自动检查清单
在工作表底部添加验证清单：
```
=IF(COUNTIF(I:I,"<>")=COUNTIF(H:H,"是"),"✓ 所有必需参数已填写","✗ 还有必需参数未填写")
```

### 2. 完成度统计
```
=CONCATENATE("完成度: ",ROUND(COUNTIF(I:I,"<>")/COUNTIF(H:H,"是")*100,1),"%")
```

### 3. 时序数据验证
对于时序数据，检查是否有24个数据点：
```
=IF(LEN(I2)-LEN(SUBSTITUTE(I2,",",""))=23,"✓ 24小时数据完整","✗ 时序数据不完整")
```

## 工作表结构建议

### 工作表1：参数模板
- 完整的参数列表和说明
- 示例值参考
- 数据来源指引

### 工作表2：数据填写
- 简化的填写界面
- 数据验证功能
- 进度跟踪

### 工作表3：时序数据
- 24小时时序参数的详细填写
- 图表可视化预览

### 工作表4：验证报告
- 数据完整性检查
- 约束条件验证
- 错误提示汇总

## 保存和导出

### 保存格式
- 工作文件：.xlsx格式
- 最终数据：.csv格式（用于导入Julia程序）

### 导出为Julia字典
可以使用Excel的VBA或Python脚本将填写的数据转换为Julia字典格式。

## 团队协作建议

1. **版本控制**：使用文件命名包含日期和版本号
2. **权限设置**：保护模板区域，只允许编辑"实际值"列
3. **审核流程**：设置数据审核和确认机制
4. **备份策略**：定期备份填写进度

---

**提示**：建议先在小范围内测试模板的实用性，根据实际使用情况调整格式和验证规则。
